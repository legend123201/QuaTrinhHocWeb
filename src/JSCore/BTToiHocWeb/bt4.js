function solution(input) {
    /*
    Tạo một Basic Calculator (chương trình máy tính đơn giản) dựa vào dữ liệu input
    */
    let tong = Number(input[0]);
    for (let i = 1; i < input.length; i += 2) {

    }
}